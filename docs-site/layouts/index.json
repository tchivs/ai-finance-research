[
  {{ range $index, $page := .Site.RegularPages }}
    {{ if $index }},{{ end }}
    {"title": {{ $page.Title | jsonify }}, "url": {{ $page.RelPermalink | jsonify }}, "text": {{ $page.Plain | jsonify }}}
  {{ end }}
]
