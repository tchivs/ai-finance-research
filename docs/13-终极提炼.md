# 终极提炼 — 最后的精华

## 写在前面

这批项目里最深的东西不在架构图里，而在那些**被 bug 逼出来的设计**里。
以下是从 BUGS-LOG、RELEASE-NOTES、特征工程、财务模型中压出来的最后几滴精华。

---

## 一、BUGS-LOG.md：反再犯checklist工程

**来源**: UZI-Skill `docs/BUGS-LOG.md`（1593行）

这是我发现的最有价值的工程文件。不是简单的 bug 列表，而是**每条bug都包含了"未来改这里要注意什么"**。

### 每条bug的标准结构

```
## BUG编号 · 简短标题

- **症状**：用户看到了什么
- **位置**：哪个文件哪段代码
- **根因**：为什么出现（3-5行深度分析）
- **影响**：哪些用户/功能受影响
- **修法**：具体改了哪些文件
- **验证**：多少测试、什么场景
- **回归测试**：测试文件名
- **未来改该区域注意事项**：⚠️ 最重要部分
```

### 真实案例：v3.8.1 加评委导致的6个静默降级缺陷

这是最经典的案例。v3.6.3/3.7.0 加了 14 位新评委后，报告出问题了：
- 头像破图、流派评分卡只显示7派（H/I消失）、组标签显示裸字母
- **但 CI 全绿**。根因：所有配套层用 `.get(g, default)` 优雅降级，不崩，所以视觉缺陷潜伏了 2 个版本

修法总结了一份 **「加新评委的 checklist」**：
```
investor_db → investor_criteria → gen_pixel_avatars.py 重跑 
→ MARKET_SCOPE → PERSONAS 台词 
→ 若新增组：GROUP_LABELS ×2 / render_school_scores order / 
  GROUP_DEFAULT / STYLE_GROUP_WEIGHTS / SCHOOL_LABELS / 
  run.py --school choices / _render_school_lock_banner THEMES / 
  score_fns GROUP_META
```

**核心教训**："优雅降级可以隐藏 bug。`.get(g, 1.0)` 让 6 个缺陷潜行了两个版本。"

### 真实案例：v3.6.3 Serenity 错杀光学股

- 水晶光电（光学光电子）被 AI 评委 Serenity 给了 `bearish / 0`
- 根因：`_AI_CHOKEPOINT_KW` 关键词库只覆盖了**数据中心光**，漏了 **AR/消费/车载光学族**
- 教训：`ar`/`vr`/`ai` 等 2-3 字母英文词不能在关键词库里用（匹配到 margin/warrant = 全局误命中）
- **永远要用中文词**（增强现实/虚拟现实）或足够特异的英文词（waveguide/micro-led）

### 价值
当前项目参考 BUGS-LOG.md 模式，每修一个 bug 必记：
1. 一条bug一个条目
2. 必须包含「症状/位置/根因/影响/修法/验证/回归测试/未来注意事项」
3. **"未来注意事项"**是防再犯的关键
4. 每个bug都关联到 `test_no_regressions.py` 的具体测试

---

## 二、RELEASE-NOTES.md：用户驱动演进的长日志

**来源**: UZI-Skill `RELEASE-NOTES.md`（3999行）

从 v2.x 到 v3.9.1 的完整演进日志，**每条更新都链接到 GitHub issue**。从中提炼出 UZI-Skill 的演进规律：

### 版本演进节奏

| 版本范围 | 重点 | 示例 |
|---------|------|------|
| v2.x | 打基础 → 出关键bug | 22 fetcher 搭建、评级准则、静态报告 |
| v3.0-v3.2 | 架构拆分 → 可维护 | pipeline、report 拆分、瘦身 |
| v3.3-v3.4 | 社区反馈 → 修补 | banner对比度修复、字体多行溢出 |
| v3.5-v3.6 | 功能扩展 → 视觉升级 | School Lock、暗黑模式、多股对比 |
| v3.7-v3.8 | 评委扩容 → 治理 | 13位科技大佬、"加评委 checklist" |
| v3.9 | 社区用户 → 真实数据 | ghzw 从真实交割单蒸馏 |

**每个版本的背后都是真实的用户问题**，不是拍脑袋功能。

### 最有价值的版本：v3.9.0 — ghzw

这是最让我震撼的条目。从 **3898 张持仓截图 → OCR → 8951 笔反推交割单**（2016-2026）+ **5069 条论坛发言** → 蒸馏为一个 F 组评委。

**定量画像**：
| 指标 | 值 |
|------|-----|
| 资产轨迹 | 33万 → 3131万（~95倍/10年） |
| 持仓周期 | 中位 1 天，均值 3.5 天 |
| 广度 | 10年操作 2010 只票 |
| 第一重仓 | 中位 51% |

**方法论蒸馏**（不是逐字转载）：
- 复盘三问：为什么涨停 / 在板块什么地位 / 在大盘什么地位
- 接力纪律：弱转强快速板才算超预期
- 选股：只做当期最强主线
- 格局票：不恐高，把代表标的当"情绪载体"

**行为复刻验证**：
- 鸿博式妖股 → bullish 100（他2023年真做过22次）
- 茅台白马 → bearish 9.5（"逆主线=送钱"）
- 美股 → skip

---

## 三、stock_features.py：特征提取层

**来源**: UZI-Skill `lib/stock_features.py`（591行）

核心设计：一个 `extract_features(raw, dims)` 函数从 raw_data 中提取 ~60 个标准化特征。

```python
def extract_features(raw: dict, dims: dict) -> dict:
    # 所有 investor criteria 规则只读 features
    # 不直接访问 raw_data
    # 特征名稳定不变
```

**包含的**特征类型：
- 财务类：roe_ttm、gross_margin、net_margin、revenue_growth、fcf_yield
- 估值类：pe_ttm、pb、ps_ttm、peg
- 技术类：price_ma20_ratio、rsi_14、volume_ratio
- 规模类：market_cap_class、float_market_cap
- 行业类：industry_sw、concept_tags、index_membership
- 衍生类：ai_chokepoint_score（AI产业链卡位分）

**安全性函数**：`_f(v, default=0.0)` 能处理 `None/""/"—"/"nan"/"N/A"/"¥"/"亿"/","` 等各种脏数据。

**原则**：
- Criteria 永远不直接读 raw_data
- 特征名是接口契约，改动需要同步 data-contracts.md + 测试
- 每条特征有明确的单位/范围/含义

---

## 四、fin_models.py：机构级财务模型

**来源**: UZI-Skill `lib/fin_models.py`（591行）

5 个纯 Python 模型，适配 A 股默认值：

### 1. DCF
```python
def compute_dcf(features, assumptions=None) -> dict:
    # WACC = CAPM: rf + beta × ERP
    # A股默认: rf=2.5%(10Y国债), ERP=6%, beta=1.0, tax=25%
    # 2-stage FCF: 高速5年 → 过渡5年 → 终值
    # 输出: 敏感性矩阵 (WACC×终值增长率, 7×7)
```

### 2. Comps
```python
def build_comps_table(target, peers) -> dict:
    # PE/PB/PS/EV-EBITDA 百分位
    # 隐含股价 (through median PE)
```

### 3. 三表预测
```python
def project_three_stmt(features, assumptions) -> dict:
    # 5年利润表/现金流量表/资产负债表
    # 勾稽关系: IS↔CF↔BS
```

### 4. LBO
```python
def quick_lbo(entry_ebitda, exit_multiple, debt_pct, years) -> dict:
    # 入口EV → 债务安排 → 退出IRR/MOIC
```

### 5. Accretion/Dilution
```python
def accretion_dilution(...) -> dict:
    # 并购 pro-forma EPS
```

**关键设计**：`methodology_log` — 每个模型返回的计算日志，让 report 能引用"为什么这个数字是这个值"。

---

## 五、segmental_model.py：分部估值（底部向上）

**来源**: UZI-Skill `lib/segmental_model.py`（408行）

对传统 DCF（自上而下×统一增速假设）的反动：**单一营收增速假设掩盖业务结构分化**。

```
传统 DCF: 整体营收 × 统一增长率假设
分部模型: segment_1(价×量×市占) + segment_2(价×量×市占) + ...
          → 总量必须 reconcile 到当前营收 ±10%
```

**适用场景**：
- 业务转型中的公司
- 多条不同增速曲线（茅台直销/经销、宁德动力/储能/海外）
- 并购+有机增长混合

**三情景模型**：Bull/Base/Bear × 3 年 projection。

---

## 六、Vibe-Trading API Server Architecture

**来源**: Vibe-Trading `agent/api_server.py`（3626行） + `agent/mcp_server.py`（1910行）

134 个 Pydantic 模型 + 相应 endpoint，覆盖：
- Agent 会话管理（CreateSession → SendMessage → SSE stream）
- Goal 生命周期（CreateGoal → AddEvidence → UpdateStatus）
- Backtest 执行与结果
- LLM 配置管理
- 数据源配置
- Mandate/Commit 工作流

MCP Server 将主要功能暴露为 MCP 工具，让 Claude/Cursor/OpenClaw 原生调用。

---

## 七、tickflow-stock-panel 本地量化工作台 Architecture

**来源**: tickflow-stock-panel `backend/app/main.py` + `tickflow/repository.py` + `strategy/engine.py` + `backtest/engine.py`

这个项目补上了一个此前缺失的最终形态：把数据底座、策略系统、实时行情、回测和 AI 报告收进一个自托管应用，而不是分散成采集脚本、研究脚本和静态报告。

最值得复用的不是某个页面，而是三层契约：

```text
CapabilitySet      — 当前环境真实能做什么
DataStore          — 数据在磁盘上如何组织，哪些目录是稳定接口
StrategyDef        — 策略如何同时服务选股、监控、回测
```

**核心教训**：个人量化产品未必一开始就需要完整数据库和复杂交易 Agent。Parquet + DuckDB + Polars + SSE 已经能支撑一个低运维、可交互、可扩展的工作台。

---

## 八、QuantDinger Agent-ready Trading OS Architecture

**来源**: QuantDinger `routes/agent_v1/` + `utils/agent_auth.py` + `utils/agent_jobs.py` + `utils/safe_exec.py` + `services/backtest_execution.py`

QuantDinger 补上的是另一种最终形态：当 Agent 不只读数据、还会创建策略、跑回测、触发交易动作时，系统必须先有硬边界。

最值得复用的是六层闸门：

```text
Agent token scope  — R/W/B/N/C/T 分能力授权
Allowlist          — markets / instruments 限定可触达范围
Idempotency        — 重试不会生成重复 job 或订单
Audit              — route / method / scope / request / response 留痕
safe_exec          — 指标/策略代码先校验和沙箱执行
paper-only gate    — 实盘需 token、环境变量、确认参数多重解锁
```

**核心教训**：金融 Agent 的产品化不是先让模型更会下单，而是先让每个动作可授权、可重放、可审计、可撤退。没有这些边界，MCP/Agent 工具越强，风险越大。

---

## 九、跨项目最后总结

### 12 个你能直接复用的工程模式

| # | 模式 | 来源 | 一句话 |
|---|------|------|--------|
| 1 | **BUGS-LOG 防再犯日志** | UZI-Skill | 每条bug必须注明"未来改这里注意"，并关联回归测试 |
| 2 | **特征提取隔离层** | UZI-Skill | Criteria 不读 raw_data，只读 features（~60个标准化字段） |
| 3 | **methodology_log 可追溯** | UZI-Skill | 所有财务模型每个中间步骤都记录为什么 |
| 4 | **distill 真实用户→评委** | UZI-Skill | ghzw: 3898截图→8951交割单→行为统计→规则→验证 |
| 5 | **加评委 checklist** | UZI-Skill | 从 6 个静默缺陷学会: 不要优雅降级，要硬断言 |
| 6 | **优雅降级是毒药** | UZI-Skill | `.get(g, 1.0)` 让缺陷潜行两版本的教训 |
| 7 | **Flat CHANGELOG + release notes 合体** | multi | CHANGELOG 用扁平格式防 merge 冲突；RELEASE-NOTES 记录完整故事 |
| 8 | **CapabilitySet + 本地数据湖 + StrategyDef** | tickflow-stock-panel | 能力、数据、策略三层契约把自托管工作台撑起来 |
| 9 | **工具调用 envelope 审计** | Privora / quant-buddy / snowball-cli | 记录 `skillId/command + params + response shape + session/version`，不要只留最终文本 |
| 10 | **静态快照先行** | hhxg-market | 日报/两融/日历/快讯先预计算成 JSON，Agent 只读和解释 |
| 11 | **金融能力拆包** | Awesome Finance Skills | 数据、搜索、情绪、预测、报告、可视化拆成独立 skill，便于复用和审计 |
| 12 | **Agent Trading Gateway 六层闸门** | QuantDinger | scope、allowlist、idempotency、audit、safe_exec、paper-only 先于交易智能化 |

### 最后提醒

> **在工程治理和防再犯这条线里，UZI-Skill 仍是最值得反复看的样本。**
> 它不只是一个技能包，而是一套完整的工程哲学：
> 每个 bug → 登记+测试+checklist
> 每个版本 → 有用户驱动、有社区链接
> 每个功能 → 有配套治理，有防再犯机制
> 这才是真正值得学的东西。
