# 补充三：数据采集与流水线 — Fetcher Registry、并发编排、网络自适应

## 来源项目
- UZI-Skill (fetcher registry, collect.py, wave-based 并发)
- daily_stock_analysis (DataFetcherManager, 策略模式+优先级+防封禁)
- Agent-Reach (Channel 抽象, probe 系统)
- tickflow-stock-panel (CapabilitySet, daily_pipeline, custom providers)
- QuantDinger (DataSourceFactory, Agent jobs, PostgreSQL 运行态, backtest range policy)
- JCP / PanWatch / magpie (桌面/盯盘运行态行情接入与缓存)
- Awesome Finance Skills / snowball-cli / hhxg-market / Privora Examples (skill/CLI/gateway/静态快照数据层)

---

## 1. UZI-Skill: Fetcher Registry — 工厂模式注册表

核心设计：22 个 fetcher 用工厂函数统一生成 adapter。

```python
def _make_adapter(
    dim_key: str,
    legacy_module: str,       # 调老 fetch_X.py
    required: list[str],      # quality=FULL 最低要求字段
    optional: list[str],      # 补充字段
    args_fn: Callable,        # 从 ticker + raw 提取老 main() 的参数
    top_level: list[str] = None,    # 溢出到 raw 顶层
    depends_on: list[str] = None,   # 依赖维度
    markets: tuple[str, ...] = ("A", "H", "U"),
) -> type:
```

### 关键设计
- **args_fn 统一抽象**: 老 fetcher 调用方式不同（有的只要 ticker，有的要 industry）
- **depends_on**: 声明前置依赖（如 industry 需要先跑 basic）
- **markets**: 声明支持的市场（A股/港股/美股）
- **quality 分级**: FULL / PARTIAL / FALLBACK

### Wave-based 并发编排 (collect.py)

```
Wave 1: 0_basic 先跑（后续 fetcher 依赖 industry）
    ↓
Wave 2: 非依赖型 fetcher 并发（max_workers=6）
    + mini_racer 串行锁（V8 isolate 非线程安全）
    ↓
Wave 3: 依赖型 fetcher（3_macro / 7_industry / 9_futures / 13_policy）
```

Feature flag 切换：
```python
def is_pipeline_enabled() -> bool:
    return os.environ.get("UZI_PIPELINE") == "1"
# 默认走老路径，UZI_PIPELINE=1 启用新管道
```

业务零区别保证：
- 输出 raw_data.json 跟 legacy 格式完全一致
- pipeline-extra 元信息放 `_pipeline` 命名空间
- 性能对齐：cold ~5min，warm ~15s

---

## 2. daily_stock_analysis: DataFetcherManager — 策略模式+优先级+防封禁

### 架构
```python
class BaseFetcher(ABC):
    PRIORITY: int
    def fetch_kline(self, code, ...) -> DataFrame
    def fetch_quote(self, code, ...) -> DataFrame
    def can_handle(self, code) -> bool   # 判断是否支持该市场/代码

class DataFetcherManager:
    def __init__(self, fetchers: Optional[List[BaseFetcher]] = None)
    # 自动故障切换 + 指数退避重试 + 并发控制
```

### 数据源优先级（动态调整）

**配置了 TUSHARE_TOKEN**：
1. TushareFetcher (priority 0)
2. EfinanceFetcher (priority 0) — 同优先级按初始化顺序
3. AkshareFetcher (priority 1)
4. PytdxFetcher (priority 2) — 通达信协议
5. BaostockFetcher (priority 3)
6. YfinanceFetcher (priority 4)

**未配置 TUSHARE_TOKEN**：
1. EfinanceFetcher 升至最高
2. Tushare 降级到 priority 2（不可用）
3. LongbridgeFetcher (priority 5) — 美股/港股兜底

### 防封禁策略
1. 每个 Fetcher 内置流控逻辑
2. 失败自动切换到下一个数据源
3. 指数退避重试机制
4. 单数据源健康状态跟踪（`daily_health`）

### 标准化列名
```python
STANDARD_COLUMNS = ['date', 'open', 'high', 'low', 'close', 'volume', 'amount', 'pct_chg']
```
所有 fetcher 输出统一列名，下游消费方无感知。

---

## 3. Agent-Reach: Channel 抽象 + Probe 系统

### Channel 基类设计（已在前文详述）

关键新增：`probe_command` 两段式探测：

```python
def check(self, config=None):
    # 两段式探测：
    # 1. 收集全部候选状态
    # 2. 第一个 ok 获胜；没有 ok 才轮到第一个 warn
    # 否则「装了但未登录」的会把排后面的完全可用的挡在门外
```

### Probe 实现
```python
# 不只是 shutil.which() — 真的执行一个轻量命令
# 检测 stale venv shim（which 能通过但实际无法执行）
probe_command(cmd, timeout=5) -> {"ok"/"warn"/"error", message}
```

---

## 4. tickflow-stock-panel: 能力探测 + 盘后增量管道

tickflow-stock-panel 不是把“有没有高级行情能力”写死在业务代码里，而是在启动时做能力探测，形成一个 `CapabilitySet`。后续功能只问能力，不问套餐名或 provider 名。

```text
detect_capabilities()
    ↓
CapabilitySet
    ↓
policy.py / tiers.yaml
    ↓
盘前、盘后、分钟K、ETF、财务、盘口等功能按能力开启或降级
```

### 盘后流水线的关键判断

`daily_pipeline.py` 的价值在于它不是无脑全量重跑。它会根据几类变化决定全量或增量：

| 触发条件 | 处理方式 |
|------|------|
| 新交易日数据到达 | 增量补最新分区 |
| 除权因子变化 | 相关 symbol 历史窗口重算 |
| enriched 缺列或字段契约变化 | 触发更大范围重算 |
| 历史数据扩展 | 从扩展日期向后补算指标 |

### 自定义行情源与扩展表分层

项目里有两类容易混淆的扩展点：

| 机制 | 作用 |
|------|------|
| custom data provider | 替换或补充行情源，服务 K 线/quote 数据 |
| `ext_data` | 上传、HTTP 拉取或解析 CSV/Excel/JSON，作为扩展分析表挂进 UI 和选股结果 |

这个分层值得保留：行情源影响基础市场数据，扩展表影响分析维度。二者不要共用一个“自定义数据”概念。

### 借鉴点

- 数据源能力应先被探测为结构化能力集，再由业务功能消费。
- 增量管道不能只按日期追加，还要感知除权、字段契约和历史窗口修复。
- 外部数据扩展要区分“行情源”和“分析表”，避免后续 API 语义混乱。

---

## 5. 网络自适应：四类机制对比

| 能力 | UZI-Skill | daily_stock_analysis | Agent-Reach | tickflow-stock-panel |
|------|-----------|---------------------|-------------|----------------------|
| 网络预检 | network_preflight.py | 运行时探测 | probe_command | detect_capabilities |
| 降级策略 | DIM_STRATEGIES 自适应 | 优先级+自动切换 | ordered_backends | CapabilitySet + policy |
| 代理检测 | HTTP_PROXY/ALL_PROXY | 无 | 代理 env 检测 | provider 配置层 |
| 缓存策略 | 分级 TTL | 无显式 | 无 | Polars enriched 热缓存 |
| 健康追踪 | _review_issues.json | daily_health | active_backend | app.state.capabilities |

---

## 6. 后两批新增：数据接入从 provider 走向 Agent 工具协议

第五、第六批项目体现了另一种数据接入趋势：不一定先做统一大型 provider，也可以先把外部数据封成 Agent 可调用的 skill、CLI、gateway 或静态快照。关键是输出要结构化，边界要可审计。

| 项目 | 数据接入形态 | 可借鉴点 |
|------|-------------|---------|
| JCP | MarketService 解析行情/K线/盘口/F10，TDX/Sina fallback，OpenClaw `/analyze` | 桌面 Agent 可在本地服务内聚合多类上下文 |
| PanWatch | TradingAgents 数据适配、portfolio context、same-day cache | 第三方框架产品化时要注入本地持仓和缓存 |
| magpie | Python worker 拉 Sina/Tencent/Eastmoney，Node 维护 quote_cache | 小型 daemon 可把采集和规则引擎分进不同进程 |
| Awesome Finance Skills | akshare/yfinance/EastMoneyDirect + SQLite `stock_prices/stock_list` | skill 内部也需要本地缓存和 fallback |
| snowball-cli | `requestPublic` 与 cookie `request` 分离，所有输出 JSON | 反向接口适合先 CLI 化，公开和登录端点分层 |
| hhxg-market | 静态 JSON + OpenAPI + `~/.cache/hhxg-market` | 盘后数据适合预计算为快照，Agent 只读 |
| Privora Examples | `POST /agent/skills/execute` + `skillId` + `params` | 数据资产访问可以通过统一 gateway 做权限和审计 |
| joinquant-skill | API references/resources/tools 分层 | 平台 API 知识应被结构化索引，而不是靠模型记忆 |
| QuantDinger | `/api/agent/v1` + `qd_agent_jobs` + `/jobs/{id}/stream` | 重型回测/实验任务应 submit+poll/stream，并保留 idempotency/audit |

### 四种可并存的数据层

```text
Raw provider
    -> fetcher registry / fallback / rate limit

Local data lake
    -> CSV/PostgreSQL/Parquet/DuckDB/SQLite

Agent tool layer
    -> MCP / CLI / skill gateway / Bash scripts

Snapshot layer
    -> static JSON / OpenAPI readonly / cache fallback
```

### 借鉴点

- 不是所有数据源都要先纳入主 provider；反向接口和第三方平台可先做 JSON CLI 或 skill gateway。
- 静态快照适合日报、日历、两融、快讯等“读多写少”的 Agent 输入。
- Agent 工具层必须保留原始参数和 response shape，方便审计和重放。
- 桌面/daemon 场景中，本地 SQLite quote_cache/watchlist/alert_history 比远端服务更轻、更稳。

---

## 当前项目行动清单

| 能力 | 参考来源 | 适用项目 | 优先级 |
|------|---------|---------|--------|
| Fetcher Registry 工厂模式 | UZI-Skill | ashare-audit | P1 |
| Wave-based 并发编排 | UZI-Skill | ashare-audit, HermesAlpha | P1 |
| 数据源优先级+自动 fallback | daily_stock_analysis | ashare-audit | P1 |
| 标准化列名/接口 | daily_stock_analysis | ashare-audit | P1 |
| CapabilitySet 能力探测 | tickflow-stock-panel | HermesAlpha / ashare-audit | P1 |
| 盘后增量 enriched 管道 | tickflow-stock-panel | 行情/指标/审计中间表 | P1 |
| 行情源与扩展分析表分层 | tickflow-stock-panel | 所有外部数据接入 | P1 |
| 防封禁流控 | daily_stock_analysis | ashare-audit | P2 |
| Probe 两段式探测 | Agent-Reach | 所有数据源接入 | P2 |
| Feature flag 切换 | UZI-Skill | 所有重大重构 | P2 |
| 数据源健康状态追踪 | daily_stock_analysis | ashare-audit | P2 |
| 明确 market category，不靠 symbol 猜数据源 | QuantDinger DataSourceFactory | 多市场行情接入 | P1 |
| 长任务统一 Agent jobs + SSE stream | QuantDinger | 回测/实验/审计运行页 | P1 |
| 桌面/daemon 本地 quote_cache + alert_history | JCP / magpie | 盯盘与桌面工作台 | P1 |
| 第三方 API 先 JSON CLI 化，区分 public/auth request | snowball-cli | 外部数据接入 | P1 |
| 静态 JSON 快照 + schema/cache/date 提示 | hhxg-market | 日报/两融/日历 | P1 |
| `skillId + params` gateway 统一数据资产调用 | Privora Examples | Agent API | P1 |
| skill 内部 SQLite 缓存股票、新闻、搜索和信号 | Awesome Finance Skills | 研究记忆 | P2 |
| 平台 API reference/resources/tools 结构化 | joinquant-skill | 平台策略生成 | P2 |
