# 知识库使用与维护指南

本文定义资料库的阅读层级、术语、来源约定和更新检查，目标是让研究结论可定位、可复核、可持续更新。

## 1. 文档职责

| 层级 | 文档 | 回答的问题 |
|------|------|------------|
| 入口 | `README.md`、`00-INDEX.md` | 有哪些内容，应该从哪里开始？ |
| 决策 | `18-模式决策矩阵.md` | 面对技术分叉时选什么，为什么？ |
| 执行 | `17-落地路线图.md` | 先做什么，交付什么，如何验收？ |
| 综合 | `10-SYNTHESIS.md`、`06~16` 专题文档 | 多个项目共同揭示了什么模式？ |
| 验证 | `19-首批源码落地验证.md` | 固定到哪个 commit，真实调用链和失败边界是什么？ |
| 证据 | `<project>/QUICK-START.md`、`DEEP-ANALYSIS.md` | 某个项目实际如何实现，有什么限制？ |

不要在入口索引堆放实现细节，也不要把单项目观察直接写成当前项目的默认架构。结论应沿“证据 → 源码验证 → 综合 → 决策 → 执行”逐层收敛。并非每个项目都要立即完成源码验证，但进入实际编码的参考模块必须固定 commit 并核对失败路径。

## 2. 结论可信度

引用资料库结论时，建议明确它属于哪一类：

| 类型 | 含义 | 使用方式 |
|------|------|----------|
| 已验证事实 | 在上游源码、配置或文档中直接观察到 | 可作为项目能力描述，仍需记录版本 |
| 跨项目模式 | 至少两个项目采用了相近做法 | 可进入综合文档，说明共同条件和差异 |
| 当前建议 | 针对 HermesAlpha 或 ashare-audit 的取舍 | 进入路线图或决策矩阵，并写明验收标准 |
| 候选模式 | 有启发但证据不足、实现不完整或尚未验证 | 明确标注“候选”，不可当作生产能力 |
| 上游限制 | 占位实现、实验性接口、缺少测试或依赖外部行为 | 与可借鉴点同时记录，避免只摘优点 |

当综合结论与项目深度分析冲突时，以项目证据为准，并修订综合文档。

## 3. 项目文档标准

每个项目目录固定包含：

```text
<project>/
├── QUICK-START.md
└── DEEP-ANALYSIS.md
```

### QUICK-START 最小结构

1. 一句话定位。
2. 来源与分析基线。
3. 核心流程或架构图。
4. 最值得借鉴的 3~7 个设计。
5. 已知限制和不应照搬的部分。
6. 指向深度分析的链接。

### DEEP-ANALYSIS 最小结构

1. 来源、版本或提交、分析日期。
2. 系统边界和关键模块。
3. 主要执行流与数据流。
4. 核心契约、状态和持久化方式。
5. 质量、安全、性能和运维风险。
6. 可迁移模式、适用条件和迁移成本。
7. 上游限制与待验证项。

### 来源块模板

```markdown
> 上游项目：<repository URL>
> 分析基线：<tag / commit / snapshot ID / unknown>
> 分析日期：YYYY-MM-DD
> 本地快照：仅在内部环境需要时记录，不作为公开来源
```

无法确认版本时写 `unknown`，不要用模糊措辞制造确定性。公开文档优先保留上游 URL、tag 和 commit，避免依赖绝对本地路径。

## 4. 核心术语

| 术语 | 统一含义 |
|------|----------|
| 数据契约 `DataContract` | 对字段、类型、主键、单位、时间语义、缺失值和来源的显式约定 |
| 工具调用信封 `ToolCallEnvelope` | 一次工具调用的名称、参数、会话、版本、认证状态、响应形态和原始结果摘要 |
| 来源追踪 `provenance` | 数据、命令、版本、引用和产物之间可回溯的关联 |
| 静态快照 `static snapshot` | 预先生成、可版本化、带数据日期和 schema 版本的只读数据产物 |
| `schema_version` | 数据结构版本，不等同于数据日期或生成时间 |
| `generated_at` | 产物生成时间，不等同于行情所属交易日 |
| `from_cache` | 当前结果是否来自缓存；必须与原始数据日期一起解释 |
| `raw_hash` | 原始响应或文件的内容摘要，用于去重和完整性核对，不替代原始证据 |
| `response_shape` | 响应顶层结构、数据类型、分页位置及错误结构的约定 |
| `scope` | token 或 Agent 被允许执行的能力范围，遵循最小权限原则 |
| `paper_only` | 动作只进入模拟环境，不能据此推断实盘安全 |
| `idempotency` | 同一请求重试不会重复创建任务、产物或订单 |
| `session` / `task_id` | 一次交互或任务的身份边界，用于防止跨任务串用状态 |
| `data_id` | 本次计算依赖的数据版本标识，用于避免结果与输入错配 |
| `citation_key` | 报告结论回链到来源记录的稳定标识 |
| `StrategyDef` | 可由选股、监控和回测共同消费的策略定义契约 |
| `CapabilitySet` | 当前运行环境真实可用能力的结构化描述，不代表理论支持能力 |
| `Doctor` | 对 provider、认证、配置、网络和依赖进行可重复诊断的入口 |

英文标识用于契约字段时保持原样；中文解释用于正文。不要为同一概念并存多个字段名，例如同时使用 `raw_payload_hash`、`payload_hash` 和 `raw_hash`。

## 5. 更新流程

### 新增项目

1. 创建项目目录及两份标准文档。
2. 补齐来源块、限制和分析日期。
3. 在 `00-INDEX.md` 的项目目录中增加一行。
4. 将可复用发现写入最相关的专题文档。
5. 只有当默认选择发生变化时，才更新 `10-SYNTHESIS.md` 或 `18-模式决策矩阵.md`。
6. 只有当实施顺序或验收标准变化时，才更新 `17-落地路线图.md`。

### 更新已有项目

1. 记录新的 tag、commit 或快照基线。
2. 区分“上游变化”和“本地理解修正”。
3. 优先修订事实和限制，再修订综合结论。
4. 检查旧结论是否仍有证据支撑。
5. 不维护易失效的行数、文件数量等手工元数据。

### 修改当前建议

1. 指向至少一份项目证据或专题归纳。
2. 写明适用前提和不适用场景。
3. 给出可验证的验收标准。
4. 涉及认证、交易、持仓和敏感数据时，补充失败模式与回退方案。

## 6. 发布前检查

- [ ] `README.md` 和 `00-INDEX.md` 的入口链接可访问。
- [ ] 每个项目目录同时存在 `QUICK-START.md` 和 `DEEP-ANALYSIS.md`。
- [ ] 新文档已加入索引，没有孤立文档。
- [ ] 相对链接按当前文件位置解析，不把代码路径误写成链接。
- [ ] 来源 URL、版本、提交或分析日期至少有一项可核对基线。
- [ ] 没有真实 token、Cookie、私钥、认证头、持仓明文或 `.env` 内容。
- [ ] 面向公开读者的文档不依赖绝对本地路径。
- [ ] 关键结论同时记录来源、限制和适用条件。
- [ ] 代码块闭合，标题层级连续，表格列数一致。
- [ ] 没有手工维护的行数等高频失效元数据。

## 7. 写作约定

- 标题说明“解决什么问题”，避免“补充一、补充二”成为唯一语义。
- 先给结论和边界，再给证据和实现细节。
- 表格用于比较稳定维度；长说明放在正文，不塞入超宽单元格。
- 路径、字段、命令和类型使用反引号；文档跳转使用 Markdown 链接。
- “推荐”必须带适用条件；“必须”必须对应风险、契约或验收标准。
- 金融结论区分事实、模型输出、Agent 判断和用户决策，不混写为单一答案。
