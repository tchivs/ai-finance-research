# 补充一：工程治理 — 自检门控、技术债务、数据完整性

## 来源项目
- UZI-Skill (self_review.py, data_integrity.py, cache.py)
- daily_stock_analysis (验证矩阵、稳定性护栏)
- ai-berkshire (financial_rigor + report_audit)
- tickflow-stock-panel (CapabilitySet, AI strategy validator)
- QuantDinger (Agent Gateway scope/audit/idempotency, safe_exec, paper-only trading gate)
- DojoAgents (工具结果 artifact、会话运行记录、可选 guardrail 与本地执行边界)
- vn.py (Gateway 回补、订单/成交分离、OMS 运行态与事件驱动边界)
- PA_Agent (数据预检、两阶段 JSON 记录、风险复算、审批票据与 paper-only ledger)
- Lean (Insight/target/risk/execution 分层与订单事件边界)
- PanWatch (预算、冷却、same-day cache、auto trigger gate)
- quant-buddy-skill / Privora Examples (session、scope、response-shape、敏感数据边界)
- snowball-cli / hhxg-market / Awesome Finance Skills (认证边界、schema/cache、引用/模型权重治理)

---

## 1. UZI-Skill 的自检门控系统

### self_review.py — 机械级自动检查

核心问题：过往只有 SKILL.md 的 "软要求"，Agent 可能跳过、可能忘、可能做半截。BUG#R10 那种「工业金属→农副食品加工」跑完报告才发现的严重问题说明 soft gate 不够。

**解决方案：机器级自动检查**

```python
# ~20 条自动检查，覆盖 BUG 经验 + 常见坑
# 输出 _review_issues.json
# critical ≠ 0 时 → 拒绝生成 HTML
```

检查项（按类别）：

| 检查 | 类别 | 触发条件 |
|------|------|---------|
| industry_mapping_sanity | industry | 行业映射异常（如工业金属→农副食品） |
| all_dims_exist | data | 必需维度缺失 |
| empty_dims | data | 维度存在但字段全空 |
| hk_kline_populated | hk | 港股 K 线为空 |
| panel_non_empty | panel | 评委评分集为空 |
| coverage_threshold | data | 数据覆盖度低于阈值 |
| placeholder_strings | data | 残留占位符文字（"待补充"等） |
| valuation_sanity | valuation | 估值计算异常（如 PE < 0） |
| consensus_formula_sanity | panel | 共识分计算异常 |
| agent_analysis_exists | self-check | agent_analysis.json 缺失 |

每个 Issue 结构：
```
severity: critical / warning / info
category: industry / data / valuation / panel / consistency / hk / self-check
dim: 关联维度 key
issue: 人读的问题描述
evidence: 触发的具体值
suggested_fix: Agent 下一步怎么处理
```

### data_integrity.py — 后采集数据完整性验证

在 Task 1（数据采集）完成后运行：

```python
CRITICAL_CHECKS = [
    ("0_basic",    "name",         "公司名称",         True),   # True = critical
    ("0_basic",    "price",        "当前股价",         True),
    ("0_basic",    "industry",     "所属行业",         True),
    ("0_basic",    "market_cap",   "总市值",           True),
    ("0_basic",    "pe_ttm",       "PE-TTM",           False),  # False = warning
    ("1_financials","roe_history",  "ROE 历史",         True),
    ...
]
```

返回：
- `critical_missing`: bool — 下游是否该重试
- `missing_dims`: list — 缺失维度清单
- `recovery_tasks`: list — 自动生成修复任务（重跑哪个 fetcher）

### 借鉴点

当前项目急需的自检能力：
1. **prompt seed 注册校验**（HermesAlpha）：每次 migration 跑完自动检查所有 seed 是否写入 DB
2. **数据源完整性检查**（ashare-audit）：akshare 返回值空字段检测，自动生成 recovery
3. **LLM provider 响应校验**：空/异常/过短 response 检测

---

## 2. 分级缓存系统 (cache.py)

UZI-Skill 的缓存设计非常精细：

```
TTL 按数据波动性分级：
├── 实时报价（price/change_pct）          60s
├── 盘中数据（K线/资金流/情绪）           5 min
├── 新闻                                    1 hour
├── 盘后聚合（龙虎榜/北向/融资融券）      2 hours
├── 季度财务/估值历史                     24 hours
└── 静态元数据（行业分类/名称）           7 days
```

功能：
- `cached(ticker, key, fetch_fn, ttl)` — 通用缓存装饰器
- `write_task_output()` / `read_task_output()` — 任务产物持久化
- `require_task_output()` — 硬 gate，前置任务未跑直接 raise
- `market_status()` — 返回交易状态（交易中/午间休市/已收盘/周末）
- `STOCK_NO_CACHE=1` 环境变量强制刷新

### 借鉴点
- ashare-audit 的数据缓存可引入分级 TTL
- HermesAlpha 的 report_daily 生成产物可用 `write_task_output` 持久化

---

## 3. 网络预检系统 (network_preflight.py)

```python
get_network_profile() → NetworkProfile
# 9 个目标分 3 组（国内 / 境外 / 搜索）
# 检测代理 env（HTTP_PROXY / HTTPS_PROXY / ALL_PROXY / NO_PROXY）
# 输出结构化 NetworkProfile 供 agent + DIM_STRATEGIES 自适应
```

### 关键设计
- 写 `.cache/_global/network_profile.json` 让 agent 介入阶段可读
- `overseas_ok` 字段 → 决定用境外源还是降级到国内源
- 代理检测 + 本地代理检测（Clash/V2Ray 等）
- 结果缓存 300 秒（`max_age_sec=300`）

### 借鉴点
- 当前项目（尤其 HermesAlpha 的多 LLM provider）可引入 provider 健康预检

---

## 4. daily_stock_analysis 的验证矩阵

```
ai-governance       AGENTS.md/CLAUDE.md 一致性     阻断
backend-gate        ci_gate.sh 脚本                 阻断
docker-build        构建验证                       阻断
web-gate            lint+build                     阻断（触发时）
network-smoke       pytest network                 观测项
pr-review           静态检查+AI审查                 辅助项
```

### 稳定性护栏条款（精选）

1. **配置修改** → 同时评估本地/Docker/GitHub Actions/API/Web/Desktop 影响
2. **新配置** → 优先「不配置也可运行，配置后增强能力」
3. **单一失败不级联** → 数据源/通知/LLM 各负责各的错误
4. **改 API/Schema/认证** → 同时检查后端/Web/Desktop 兼容性
5. **改报告/Prompt/通知** → 检查上下游兼容性
6. **CI 未覆盖** → 必须补充说明本地验证与缺口

---

## 5. ai-berkshire 的财务严谨工具链

### financial_rigor.py
- 零外部依赖（仅 Python stdlib）
- 市值验证、估值验证、多源交叉验证
- Benford 法则财务造假检测
- 三情景估值（乐观/基准/悲观）

### report_audit.py
```
Step 1: 提取数据点 → 随机抽样 15%
Step 2: AI 从可靠信源重新取数 → 填入 fetched_value
Step 3: 比对 → 差异 > 2% 打回 → 标注原因
```

### 借鉴点
- 这两个工具可直接复用到 ashare-audit
- report_audit 的抽检思想可用于：LLM 生成数据的验证

---

## 6. tickflow-stock-panel 的能力探测治理

tickflow-stock-panel 把“当前环境能做什么”抽成 `CapabilitySet`，而不是让每个 API 或页面自己判断 token、套餐、provider、数据目录是否存在。

```text
detect_capabilities()
    ↓
app.state.capabilities
    ↓
API / 调度器 / 前端功能开关 / 数据同步策略
```

这种做法本质上是工程治理：功能降级有统一依据，调试时也能直接看能力集，而不是追踪散落在各处的 if 判断。

同一项目里的 AI 策略生成也体现了类似边界：AI 可以生成策略代码，但必须经过 AST 白名单和危险调用拦截后才能落入策略目录。生成能力和执行能力被明确隔离。

### 借鉴点

- LLM provider、行情 provider、通知渠道、交易连接器都可以先探测成能力集。
- 页面灰显/隐藏、调度任务启停、API 报错文案都应该读同一个能力对象。
- AI 生成的可执行内容必须经过确定性校验，不能直接进入运行路径。

---

## 7. 后两批新增：工具调用治理从“能跑”升级为“可审计”

第五、第六批项目共同补上了一个此前文档里较弱的治理层：当金融能力以 skill、CLI、gateway 或 daemon 暴露给 Agent 时，治理对象不只是代码质量，还包括认证、预算、会话、缓存、响应形态和敏感数据边界。

| 项目 | 治理机制 | 可借鉴点 |
|------|---------|---------|
| PanWatch | 月预算、trigger cooldown、same-day cache、enable gate | AI 分析和自动提醒必须有成本/频率控制 |
| magpie | alert_rules/alert_history、cooldown、`test-fire` | 监控规则必须可测试、可追踪、可去重 |
| quant-buddy-skill | `session_id`/`task_id`/`data_id`/`SKILL_VERSION`/TopN 结果重排检查 | 工具执行要防串任务、防旧版本、防错误排序 |
| Privora Examples | Bearer Token scope 矩阵、response shape 验证、DEK 缺失识别 | API quickstart 也要体现最小权限和敏感数据边界 |
| snowball-cli | 公开接口优先，登录失败后才提示用户自行认证 | Agent 不应替用户执行认证动作 |
| hhxg-market | `schema_version`、缓存兜底、最近交易日日期提示 | 静态快照必须审计版本、日期和缓存来源 |
| Awesome Finance Skills | citation key、search cache、模型权重路径限制 | skill 内部也要保留引用、缓存和模型加载边界 |
| QuantDinger | R/W/B/N/C/T scope、market/instrument allowlist、`qd_agent_audit`、paper-only 默认 | Agent 接入交易系统时必须把权限、审计、幂等和资本风险拆开 |

### 借鉴点

- 工具调用日志至少记录：工具名、版本、参数、会话、响应 shape、是否缓存、是否需要认证。
- 自动触发类任务必须有预算、冷却和 enable gate，不能只依赖 cron。
- 所有认证相关流程都应写进 `SKILL.md` 或 quickstart，并限制 Agent 代操作。
- 预测/报告类 skill 要把 base result、Agent adjustment、citation、model checkpoint 分开记录。

---

## 8. PA_Agent：从模型结论到 paper action 的二次取证

PA_Agent 将 LLM 输出留在结构化 `AnalysisRecord` 中，再经 proposal、风险复算和审批票据进入 paper runtime。最关键的不是“AI 给出下单意见”，而是批准时不信任旧结论：审批票据消费前重新采集价格、账户、费率、挂单、频率和日内损失证据，并重新运行纯函数 `RiskEngine`。

```text
validated AnalysisRecord
    -> immutable candidate intent
    -> fresh evidence + deterministic risk assessment
    -> expiring ApprovalTicket
    -> one-time dispatch permit
    -> paper gateway + ledger projection
```

### 借鉴点

- 把分析生成时的证据和批准时的证据区分开；价格、账户与风险状态必须在动作前刷新。
- `RiskAssessment` 应为无 UI、无网络副作用的纯函数，输入为 immutable intent、policy 和 evidence bundle，输出为可复算的 reason codes。
- 票据必须绑定 candidate、target、policy 和 evidence digest；任何变化、过期或刷新失败都 fail closed。
- 当前项目只实现 paper-only 路径；真实 broker、真实凭据和真实资金均不进入 Agent 工具面。

---

## 当前项目行动清单

| 能力 | 参考来源 | 适用项目 | 优先级 |
|------|---------|---------|--------|
| 机械级自检（~20 规则） | UZI-Skill self_review | 所有项目 | P1 |
| 分级缓存（TTL 按数据波动性） | UZI-Skill cache | ashare-audit | P1 |
| 网络/Provider 预检 | UZI-Skill network_preflight | HermesAlpha | P1 |
| CapabilitySet 能力探测 | tickflow-stock-panel | provider/行情/LLM/通知功能开关 | P1 |
| AI 生成代码 AST 白名单 | tickflow-stock-panel | 策略生成/规则生成 | P1 |
| Agent Gateway scope + audit + idempotency | QuantDinger | Agent 工具层 / 交易相关接口 | P1 |
| 高风险 Agent 动作默认 paper-only，实盘需环境开关和显式确认 | QuantDinger | 交易/通知/外部写入 | P1 |
| 数据完整性 gate | UZI-Skill data_integrity | ashare-audit | P1 |
| 验证矩阵 + 稳定性护栏 | daily_stock_analysis | 所有项目的 AGENTS.md | P1 |
| 财务数据严谨校验 | ai-berkshire financial_rigor | ashare-audit | P1 |
| 报告抽检审计 | ai-berkshire report_audit | HermesAlpha 报告 | P2 |
| 自动触发预算/冷却/启停门控 | PanWatch / magpie | 盯盘和通知系统 | P1 |
| 工具调用 session/version/response-shape 审计 | quant-buddy-skill / Privora Examples | ashare-audit | P1 |
| 认证边界：公开接口优先，登录由用户显式执行 | snowball-cli | 外部数据源接入 | P1 |
| 静态快照 schema/date/cache 检查 | hhxg-market | 日报/两融/日历数据 | P1 |
| 报告引用、搜索缓存、模型权重来源治理 | Awesome Finance Skills | HermesAlpha / ashare-audit | P1 |
| 分析记录、二次取证、审批票据与 paper dispatch permit | PA_Agent | Phase 5-6 风控与 paper-only 工作区 | P1 |
