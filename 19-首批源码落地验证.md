# 首批源码落地验证：数据底座、策略工作台与 Agent Gateway

本文不是新的横向项目总结，而是对第一批落地参考源码进行基线固定和实现边界核对。验证对象是 `daily_stock_data`、`tickflow-stock-panel` 和 `QuantDinger`。

## 1. 验证结论

三份既有深度分析的架构判断总体成立，可以继续作为开发参考，但以下结论必须收紧：

1. `daily_stock_data` 的日线 provider 是“异常 fallback”，不是完整的“异常或空结果 fallback”。
2. CSV/PostgreSQL `both` 是顺序双写，不具备跨后端事务一致性。
3. tickflow 的 `StrategyDef` 确实被选股、监控和回测复用，但三个场景并非完全相同的执行语义。
4. QuantDinger 的 HTTP Agent Gateway 实盘路径有 scope、token `paper_only` 和环境开关三层门禁；显式确认字段只存在于 MCP 包装层。
5. QuantDinger 的幂等机制适合降低普通重试风险，但不是围绕外部副作用的严格事务幂等。
6. QuantDinger 存在子进程执行函数，但当前主调用链仍使用进程内 `exec`，不能按强隔离沙箱理解。

因此，最值得复制的是契约和分层方式；涉及 fallback、双写、幂等、任务执行和代码隔离时，应采用比参考源码更严格的实现。

## 2. 验证基线

| 项目 | Commit | Commit 日期 | 工作树 | 验证日期 |
|------|--------|-------------|--------|----------|
| daily_stock_data | `e0efc471e5350e08c58018cb49ba0e215cf1fb92` | 2026-06-25 | clean | 2026-07-10 |
| tickflow-stock-panel | `3656fedc628f84b3f14e41d17f9e5529bd9dc800` | 2026-07-06 | clean | 2026-07-10 |
| QuantDinger | `fc33395cbbf162e0145372341740b9ae4bac3f6a` | 2026-07-03 | clean | 2026-07-10 |

### 验证方式

- 核对真实入口、核心类型、调用链、持久化和测试文件。
- 对照现有 `QUICK-START.md` 与 `DEEP-ANALYSIS.md` 的关键主张。
- 检查失败路径，而不只检查正常流程。
- 尝试运行聚焦 pytest；当前系统 Python 未安装 pytest，未形成运行时通过结论。

本文中的“已验证”表示静态源码与调用关系已确认，不等于测试已在本机执行通过。

## 3. daily_stock_data

### 3.1 已验证的实现

| 主张 | 源码证据 | 结论 |
|------|----------|------|
| CSV/PG/both 三模式 | `scripts/storage_common.py:storage_backend()` | 成立；默认 CSV，别名会归一到 `postgres` |
| CSV 原子替换 | `write_csv_table()` | 成立；临时文件写完后调用 `os.replace()` |
| CSV append-upsert | `append_upsert_csv()` | 成立；全表合并后按 key 保留最后一条 |
| 日期窗口替换 | `replace_csv_slice()` | 成立；只删除目标 symbol 和时间窗口 |
| 日线增量修复 | `get_sync_ranges()` | 成立；新日期与最近 `repair_days` 缺口合并为抓取区间 |
| provider 顺序 | `download_daily()` | Tushare → TickFlow → baostock；指数直接 baostock |
| PostgreSQL upsert | `save_df()` | 成立；使用 `(time, symbol)` 唯一键和 `ON CONFLICT` |
| CI 质量门 | `.github/workflows/ci.yml` | ruff、编译、pytest、shell 语法和私有路径扫描均已配置 |

### 3.2 关键边界

#### 空结果不会继续 fallback

`download_daily()` 在 provider 没有抛异常时直接 `return df`。即使 `df.empty`，也不会继续调用下一个 provider。

```text
异常       -> 尝试下一个 provider
空结果     -> 直接结束
有效数据   -> 直接结束
```

自己的实现应显式区分：

```text
ProviderResult.status = ok | empty | unavailable | invalid
fallback_on_empty     = true | false
```

交易日内目标股票空结果通常应继续降级；停牌、未上市或确实无数据则需要可解释的 `empty_reason`，不能全部当成 provider 故障。

#### `both` 不是事务双写

`save_df()` 先写 CSV，再写 PostgreSQL。PG 写入失败时 CSV 已经提交，两端会出现短期或永久不一致。当前没有 outbox、补偿事务或自动对账状态。

建议自己的项目选择一个主真源：

```text
轻量本地版：Parquet/CSV 为主真源，异步导入 PostgreSQL
服务端版：PostgreSQL 为主真源，CSV/Parquet 作为导出或分析副本
```

如果保留 `both`，至少记录 `sync_run_id`、两端写入状态和对账结果。

#### 原子替换不等于并发安全

`os.replace()` 能避免半文件，但 `append_upsert_csv()` 仍是“读全表 → 合并 → 覆盖”。多个进程并发执行时可能发生丢失更新，且数据量增长后是 O(n) 读写。

### 3.3 测试覆盖判断

当前 `tests/test_storage_common.py` 有两个测试，覆盖：

- 相同主键由新记录覆盖。
- 区间替换不会误删其他股票或窗口外数据。

尚未在该测试文件中覆盖：

- 并发写入。
- `both` 模式部分失败。
- provider 空结果 fallback。
- PostgreSQL upsert 和去重建索引。
- 上游单位、复权和时区一致性。

### 3.4 对自己项目的采用结论

直接采用：数据契约、主键语义、repair window、原子文件替换、开源泄漏 CI。

改造后采用：provider fallback、CSV append-upsert、both 双写。

不直接采用：把全量 CSV 重写作为分钟线、逐笔或长期高频数据的主存储。

## 4. tickflow-stock-panel

### 4.1 已验证的实现

| 主张 | 源码证据 | 结论 |
|------|----------|------|
| `CapabilitySet` 是业务能力真源 | `tickflow/capabilities.py` | 成立；提供 `has/limits/require/to_dict` |
| 真实能力探测 | `tickflow/policy.py:detect_capabilities()` | 成立；含 schema 版本缓存、真实 probe 和档位识别 |
| Parquet 是磁盘真相 | `tickflow/repository.py:DataStore` | 成立；DuckDB 使用内存连接，视图读取 Parquet glob |
| 稳定目录契约 | `DataStore.__init__()` | 成立；行情、enriched、财务、回测、用户状态等目录集中创建 |
| 策略统一加载 | `strategy/engine.py:StrategyDef` | 成立；统一 meta、过滤、信号、止损、持有期和来源 |
| 选股执行 | `StrategyEngine.run()` | 成立；基础过滤 → 策略过滤 → 评分排序 |
| 策略回测 | `StrategyBacktestService.run()` | 成立；复用 StrategyDef，并加入 entry/exit、成本和撮合约束 |
| 策略监控 | `MonitorRuleEngine._match_strategy()` | 成立；复用选股引擎并比较前后选股池 |
| Pipeline job 留痕 | `services/pipeline_jobs.py:JobStore` | 部分成立；终态写 JSON，活跃态只在内存 |

### 4.2 关键边界

#### 能力探测可能把基础设施故障降级成“无能力”

`detect_capabilities()` 最外层捕获所有异常并返回 `none` 档。这个行为保证应用能启动，但网络故障、配置错误、probe 代码缺陷都可能被表达成能力缺失。

自己的能力模型应同时返回：

```text
availability = available | unavailable | unknown
reason       = no_key | invalid_key | network_error | provider_error | stale_cache
checked_at   = timestamp
```

关键能力不能只用 `has(cap) == false` 表达所有失败原因。

#### StrategyDef 复用不等于三个场景语义完全一致

- 选股使用 basic filter、`filter/filter_history` 和评分。
- 回测额外使用 entry/exit signals、止损、止盈、持有期、费用和撮合模型。
- 当前策略监控重新运行选股逻辑，比较本轮与上轮候选池，产生 `new_entry/dropped`；它不是回测 entry/exit 的实时重放。

因此更准确的说法是“共享策略定义和候选逻辑”，而不是“选股、监控、回测结果天然一致”。自己的系统需要为每种执行器写一致性测试。

#### JobStore 适合单进程本地工作台

`JobStore` 的 active job、锁和进度日志位于进程内；只有 succeeded/failed 终态写入独立 JSON。它还会复用当前 running job，只允许一个活动 pipeline job。

这适合桌面或单实例服务，但不能直接扩展到多 worker：

- 进程重启会丢失 active 状态。
- 多进程各自拥有 `_active_id`。
- JSON 文件没有跨进程事务和锁。
- SSE 或轮询看到的活跃状态取决于请求落到哪个进程。

### 4.3 测试覆盖判断

仓库有回测成本、组合撮合和策略正确性测试，也有 AI provider、插件 provider 测试。现有深度分析指出的 pipeline、monitor、ext_data 聚焦测试不足仍成立。

### 4.4 对自己项目的采用结论

直接采用：能力对象、数据目录契约、Parquet + DuckDB 分层、策略定义对象、回测约束拆分。

改造后采用：能力降级状态、StrategyDef 跨执行器复用、JobStore。

不直接采用：把进程内 active job 或单个 DuckDB connection 当作多实例协调机制。

## 5. QuantDinger

### 5.1 已验证的实现

| 主张 | 源码证据 | 结论 |
|------|----------|------|
| Agent token scope | `utils/agent_auth.py:agent_required()` | 成立；校验 token、状态、过期、scope 和 rate limit |
| 每次调用审计 | `agent_required()` / `_audit()` | 成立；成功和拒绝路径都会写审计摘要 |
| market/instrument allowlist | `market_allowed()` / `instrument_allowed()` | 成立 |
| 长任务持久化 | `utils/agent_jobs.py:submit_job()` | 成立；先写 PG queued row，再交给进程内线程池 |
| SSE 进度 | `stream_progress()` | 成立；进程内 ring buffer 支持 `since_seq`，PG 保存最新 snapshot |
| 默认 paper | `agent_v1/quick_trade.py:place_order()` | 成立；不满足 live 条件时记录 paper order |
| 代码安全检查 | `safe_exec.py` | 成立；builtins/import/AST/属性/I/O 路径均有限制 |

### 5.2 HTTP 与 MCP 的确认边界

HTTP Agent Gateway 的 live quick trade 实际要求：

1. 路由通过 `@agent_required(SCOPE_T)`。
2. token 的 `paper_only=false`。
3. `AGENT_LIVE_TRADING_ENABLED=true`。
4. 请求通过 market、instrument、数量、credential 和 broker policy 校验。

`place_order()` 本身没有检查 `confirm_order` 或 `confirm_live_trading`。这两个确认字段由 MCP 工具 `place_quick_order()` 在调用 HTTP Gateway 前检查。

这意味着确认是“客户端包装层门禁”，不是服务端统一门禁。自己的系统应把资金动作确认放在 Gateway 服务端，并将 MCP/UI/CLI 的确认作为额外一层，而不是唯一一层。

### 5.3 幂等是 best-effort，不是副作用事务

`with_idempotency()` 先查询已有 `(agent_token_id, kind, idempotency_key)`。查询数据库异常时会记录 warning 后继续执行，即 fail-open。

同步 quick trade 的顺序是：

```text
查询幂等记录
    -> 调用交易所或写 paper order
        -> record_completed_job() 写完成记录
```

并发同 key 请求可能同时通过前置查询；即使唯一索引最终阻止第二条完成记录，外部订单副作用也可能已经发生。

自己的高风险幂等应采用：

```text
事务内预占 idempotency key，状态 processing
    -> 只有预占成功的请求执行副作用
        -> 使用稳定 client_order_id 调用外部系统
            -> 写 completed / failed，可恢复查询
```

数据库不可用时，高风险动作应 fail-closed。

### 5.4 Job 模型的部署边界

`submit_job()` 的 job row 和最新 progress 在 PostgreSQL，但执行器和事件 ring buffer 在当前进程。进程崩溃后 queued/running job 不会由其他实例自动接管；多实例 SSE 也需要 sticky routing 或共享事件系统。

本地单实例可以沿用；服务端多实例应换成持久队列、worker lease 和共享事件流。

### 5.5 沙箱边界

当前主调用链是：

```text
validate_code_safety()
    -> safe_exec_with_validation()
        -> safe_exec_code()
            -> 当前 Python 进程内 exec
```

`safe_exec_isolated()` 确实实现了 multiprocessing、timeout 和 Linux `RLIMIT_AS`，但当前没有调用者。它属于存在但尚未接入主路径的能力。

因此自己的项目不能只复制 validator 后就接收不可信代码。生产多租户场景至少使用独立进程，优先考虑容器、受限运行时或专用 sandbox service。

### 5.6 测试覆盖判断

仓库已有 Agent scope、job progress、safe-exec、交易所参数、策略执行和 MCP confirmation 等大量测试文件。由于当前环境没有安装 pytest，本轮未执行，结论只确认了测试资产和静态调用关系。

### 5.7 对自己项目的采用结论

直接采用：Human/Agent 双入口、scope 分类、审计摘要、JobRecord、paper 默认、market/instrument allowlist。

改造后采用：idempotency、SSE ring buffer、代码沙箱、live gate。

不直接采用：客户端独占确认门禁、幂等查询 fail-open、进程内 worker 用于多实例、高风险代码进程内执行。

## 6. 自己项目的首版契约

第一阶段不需要复制三个项目的完整代码，只需要先固化五个契约。

### 6.1 ProviderResult

```text
provider
status: ok | empty | unavailable | invalid
data_date
fetched_at
from_cache
empty_reason
error_code
raw_hash
```

### 6.2 DataContract

```text
dataset
schema_version
primary_key
timezone
units
source_fields
nullable_fields
quality_rules
```

### 6.3 ToolCallEnvelope

```text
call_id
tool / route / command
params
session_id
auth_scope
idempotency_key
started_at / finished_at
status
response_shape
raw_hash
```

### 6.4 JobRecord

```text
job_id
kind
owner
status: queued | running | succeeded | failed | cancelled | orphaned
worker_lease
progress_seq
request_ref
result_ref
created_at / started_at / finished_at
```

### 6.5 ActionGuard

```text
required_scope
resource_allowlist
paper_only
server_confirmation
deployment_kill_switch
idempotency_state
audit_required
```

## 7. 推荐的第一个垂直切片

```text
一个公开数据 provider
    -> ProviderResult
    -> static snapshot / Parquet
    -> DataContract 校验
    -> ToolCallEnvelope 留痕
    -> 只读查询 API
    -> UI 显示日期、来源、缓存和质量状态
```

这个切片通过后，再加入第二 provider 验证 fallback，再加入长任务验证 JobRecord，最后才进入 Agent 编排和高风险动作。

## 8. 本轮未完成项

- 未安装项目依赖，聚焦 pytest 未执行。
- 未启动 PostgreSQL、Redis、TickFlow API 或交易所连接。
- 未做真实数据采集、回测结果或浏览器 UI 验证。
- 未验证上游远端是否有晚于本地 commit 的更新。

这些限制不影响架构边界判断，但在复制具体函数或接口前仍需按目标模块建立可运行环境。
