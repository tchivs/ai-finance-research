# 模式决策矩阵 — 什么时候选哪种架构

> 这份文档回答“具体技术选择怎么定”。`10-SYNTHESIS.md` 讲有哪些模式，`17-落地路线图.md` 讲怎么落地，本文件讲遇到分叉时如何选择。

## 0. 快速结论

| 问题 | 默认选择 | 升级条件 |
|------|----------|----------|
| 公开或第三方数据接入 | static JSON 或 JSON CLI | 需要多用户、权限和服务 SLA 时升级为 API/Gateway |
| 本地状态 | SQLite | 出现多用户、并发写和细粒度权限时升级为 PostgreSQL |
| 历史分析数据 | Parquet + DuckDB | 需要强事务、在线高并发写时改用数据库 |
| 长任务进度 | submit + poll/SSE | 只有明确双向实时交互需求时使用 WebSocket |
| Agent 编排 | 单 Agent 或 Moderator | 流程稳定且需要 checkpoint/replay/branch 时使用 LangGraph |
| AI 建议 | suggestion pool + 人工确认 | 完成权限、风控、审计和合规评审后再讨论自动执行 |
| 组合权重 | 受约束 long-only 优化 + 离散调仓计划 | 有稳定样本外证据、市场规则 adapter 和审批后再扩大策略空间 |
| 报告可信度 | citation + data quality banner | 涉及高风险决策时增加 claim-to-source 和人工复核 |

默认选择不是永久架构。只有当升级条件真实出现，并且当前方案无法通过局部扩展解决时，才引入更重的组件。

## 1. 使用原则

不要因为某个开源项目很完整，就把它的全部架构搬过来。多数情况下，当前项目只需要吸收某一层：接口形态、数据存储、Agent 编排、审计 envelope 或 UI 交互。

决策顺序建议固定为：

```text
1. 数据是否稳定、可追溯？
2. 工具调用是否可记录、可重放？
3. Agent 是否真的需要复杂编排？
4. 用户是否需要实时 UI，还是静态报告足够？
5. 审计深度是否覆盖风险点？
```

## 2. 接口形态选择

| 形态 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|------|----------|------------|----------|----------|
| MCP | 需要让 Claude/OpenClaw/Cursor 原生发现和调用工具 | 内部私有调用、复杂认证、多步骤状态任务 | Vibe-Trading / Agent-Reach / wudao-mcp / joinquant-skill | HermesAlpha 后期暴露稳定工具；ashare-audit 暴露只读审计工具 |
| JSON CLI | 反向接口、第三方网站、开发期快速接入、需要命令 provenance | 高并发服务端、多用户 API、强权限隔离 | snowball-cli / quant-buddy-skill | 外部数据源优先 CLI 化，保留 command + raw JSON |
| Agent Gateway | Agent 可创建任务、写入状态、运行回测或触发高风险动作；需要 scope、幂等和审计 | 只有只读查询或单个简单工具 | QuantDinger / Privora Examples | 用于有副作用或长任务的 Agent API，默认 sandbox / `paper_only` |
| Skill Gateway | 多个确定性能力需要统一发现和调用；以 `skillId + params` 路由 | 强状态工作流、交易动作、复杂任务生命周期 | Privora Examples | 用于统一只读或低风险能力入口，复用 ToolCallEnvelope |
| OpenAPI 只读接口 | 公开数据、低风险查询、可被自动工具化 | 需要登录、敏感持仓、状态变更 | hhxg-market / PanWatch | 静态快照、日历、两融、快讯适合只读 OpenAPI |
| SKILL.md 本地 skill | Agent 使用说明、触发词、回答范式、工具边界 | 需要强服务 SLA 或多人权限系统 | Awesome Finance Skills / hhxg-market / magpie | 所有 Agent 工具都应有 `SKILL.md` 契约 |

### 推荐默认

```text
外部第三方数据源       -> JSON CLI
内部多能力 API          -> Agent Gateway / Skill Gateway
公开只读数据            -> OpenAPI / static JSON
Agent 安装与触发说明     -> SKILL.md
成熟稳定工具生态暴露     -> MCP
```

## 3. 数据存储选择

| 存储 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|------|----------|------------|----------|----------|
| CSV | 开箱即用、低频日线、小规模研究、迁移初期 | 高频、并发写、大数据量、复杂查询 | daily_stock_data | 第一版数据样例和导出可用，必须原子写和 upsert |
| PostgreSQL | 长期运行、多用户、权限、事务、复杂查询 | 个人单机试用、轻量 skill 分发 | daily_stock_data / PanWatch | 持仓、用户配置、长期任务状态适合 PG |
| PostgreSQL 运行态 | 策略、订单、持仓、回测、Agent jobs/audit、token 权限 | 只读静态快照或轻量 CLI | QuantDinger | 交易/审计运行态应落 PG，不应只在内存里 |
| Parquet + DuckDB | 本地数据湖、历史行情、审计窄表、低运维分析 | 高频并发写、强事务业务表 | tickflow-stock-panel | 行情和审计中间表优先考虑 |
| Polars 热计算 | 技术指标、策略筛选、宽表运行时计算 | 需要长期存储真相 | tickflow-stock-panel | enriched 窄表落盘，指标运行时计算 |
| SQLite | 本地状态、watchlist、alert_history、search_cache、引用库 | 多用户高并发、复杂权限 | magpie / Awesome Finance Skills / JCP | 盯盘、搜索缓存、信号库、citation store 很适合 |
| static JSON | 日报快照、日历、两融、快讯、公开只读数据 | 盘中实时交易、敏感数据、需频繁更新的数据 | hhxg-market | 第一阶段强烈推荐，降低 Agent 即兴拼数据风险 |

### 推荐默认

```text
日报/日历/两融/快讯      -> static JSON
自选股/提醒/搜索缓存      -> SQLite
历史行情/审计中间表       -> Parquet + DuckDB + Polars
用户/持仓/长期业务状态     -> PostgreSQL
样例/导出/迁移对账         -> CSV
```

## 4. Agent 编排选择

| 编排 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|------|----------|------------|----------|----------|
| 单 Agent + 工具调用 | 简单查询、固定工具、短流程 | 多视角冲突、复杂状态、需要可追溯讨论 | snowball-cli / hhxg-market | 查询数据、拉快照、执行单个审计检查 |
| Moderator 会议室 | 个股研究、多专家视角、需要解释谁参与和为什么参与 | 纯机械任务、简单数据查询 | JCP | HermesAlpha 个股深度研究优先采用 |
| BaseAgent 生命周期 | 盯盘、自动任务、通知、可调度 Agent | 复杂多角色辩论 | PanWatch | 盯盘 Agent 用 `collect/analyze/notify` |
| LangGraph 状态机 | 多阶段、多角色、需要 checkpoint/replay/branch | 早期原型、流程尚未稳定 | TradingAgents / Vibe-Trading / DeepFund | 后期产品稳定后再引入，不作为第一阶段默认 |
| 多 Agent 并行团队 | 财报精读、行业研究、正反方观点 | 高频自动触发、成本敏感任务 | ai-berkshire / TradingAgents | 低频深度研究使用，需预算和结果审计 |

### 推荐默认

```text
简单工具任务      -> 单 Agent + 工具
个股研究          -> Moderator 会议室
盯盘/通知         -> BaseAgent 生命周期
复杂可回放工作流   -> LangGraph
低频深度研究       -> 多 Agent 团队
```

## 5. UI 形态选择

| UI 形态 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|---------|----------|------------|----------|----------|
| 单文件 HTML 报告 | 可分享深度报告、审计报告、低部署成本 | 需要反复操作、实时状态、权限管理 | UZI-Skill | 报告导出和审计快照非常适合 |
| React 工作台 | 高频使用、看板、策略、回测、监控、设置 | 只需要一次性输出的脚本 | tickflow / daily_stock_analysis / PanWatch | HermesAlpha 第一版 UI 应是工作台，不是营销页 |
| Trading OS 工作台 | 策略生成、回测、paper/live gate、token scope、job stream 同台 | 只有研究报告或只读数据 | QuantDinger | 涉及交易动作的 UI 必须显示权限、沙箱和实盘闸门 |
| Markdown/JSON 输出 | CLI、skill、静态快照、Agent 消费 | 普通终端用户复杂交互 | hhxg-market / ai-berkshire | 数据工具默认输出 Markdown + JSON 双形态 |
| CLI/daemon 控制面 | 轻量自托管、本地监控、通知工具 | 复杂可视分析、多人协作 | magpie / snowball-cli | 盯盘最小版可先用 HTTP/CLI 控制面 |
| 运行 Timeline | 长任务、Agent 推理、工具调用、失败重试 | 瞬时查询 | Vibe-Trading / daily_stock_analysis | HermesAlpha 和 ashare-audit 都需要 |
| Provider 设置页 | 外部数据源、token、scope、成本、能力检测 | 无外部依赖系统 | snowball-cli / Privora / Agent-Reach | 第一阶段就应做最小诊断页或 `/doctor` |

### 推荐默认

```text
研究/审计最终产物     -> 单文件 HTML 或 Markdown 报告
日常操作             -> React 工作台
工具输出             -> Markdown + JSON
盯盘最小可用          -> CLI/HTTP daemon 控制面
长任务透明化          -> Timeline / RunFlow
数据源管理            -> Provider 设置页 / Doctor
```

## 6. 实时性选择

| 实时机制 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|----------|----------|------------|----------|----------|
| static snapshot | 盘后日报、日历、两融、快讯、低风险公开数据 | 秒级行情、订单执行 | hhxg-market | 第一阶段优先，最稳 |
| cron / scheduler | 日报、定时复盘、定时抓取 | 事件触发提醒、盘中秒级反应 | daily_stock_analysis / PanWatch | 盘后和周期任务使用 |
| daemon polling | 本地盯盘、价格提醒、少量自选股 | 大规模行情分发 | magpie | 轻量盯盘可用 |
| SSE | 单向推送行情、任务进度、告警、复盘进度 | 双向交互、低延迟交易 | Vibe-Trading / tickflow / OpenAshare | 工作台实时状态优先 SSE |
| Job stream | 回测、实验、审计等长任务，需断线恢复和最终 result | 毫秒级实时行情或双向协作 | QuantDinger / quant-buddy | 长任务默认 submit+poll/stream，不挂同步 HTTP |
| WebSocket | 双向实时交互、多人协作、交易控制 | 只读进度和行情 | 通用模式 | 暂不作为默认，除非有双向需求 |

### 推荐默认

```text
盘后数据         -> static snapshot
定时任务         -> scheduler
本地盯盘         -> daemon polling
工作台进度/行情   -> SSE
双向交互         -> WebSocket
```

## 7. 审计深度选择

| 审计层 | 检查什么 | 参考项目 | 什么时候必须做 |
|--------|----------|----------|----------------|
| 数据字段审计 | 必填字段、空值、单位、时间、来源 | UZI / daily_stock_data | 所有报告和策略输入 |
| Source health 审计 | provider 是否降级、延迟、失败、fallback | Agentic China Data / TDX / a-stock-data | 多数据源系统 |
| Tool envelope 审计 | command/skillId、params、session、version、auth、response shape | Privora / quant-buddy / snowball | 所有 Agent 工具调用 |
| Agent scope 审计 | R/W/B/N/C/T、market/instrument allowlist、paper_only、rate limit、audit | QuantDinger / Privora | Agent 可触发写入、回测、交易或凭证动作 |
| 策略 AST 审计 | 未来函数、非法 API、文件/网络、交易成本 | joinquant / tickflow / QuantDinger | AI 生成或外部导入策略 |
| 公式结果审计 | 条件冻结、data_id、TopN 排序、resume | quant-buddy | 公式选股、批量执行 |
| Claim-to-source 审计 | 报告判断是否有证据、引用是否完整 | UZI / Awesome / ai-berkshire | 投研报告、审计报告 |
| 敏感数据审计 | token、scope、DEK、持仓明文、日志脱敏 | Privora / snowball | 持仓、账户、登录数据 |

### 推荐默认

ashare-audit 的第一优先级不是“多写几条规则”，而是先落 `ToolCallEnvelope` 和 Agent scope audit。没有 envelope 和 scope 记录，策略 lint、报告引用、数据质量都缺少统一追溯入口。

## 8. 策略和信号选择

| 模式 | 适合用在 | 不适合用在 | 参考项目 | 当前建议 |
|------|----------|------------|----------|----------|
| StrategyDef | 选股、监控、回测共用同一规则 | 一次性临时筛选 | tickflow | HermesAlpha 策略主契约 |
| 四向信号列 | 多空、现货/合约、开平仓语义需要统一 | 简单一次性 buy/sell 脚本 | QuantDinger | 新策略接口优先 `open_long/close_long/open_short/close_short` |
| YAML 策略声明 | 非开发者可配置、策略较稳定 | 复杂 Python 计算策略 | daily_stock_analysis | 早期可用，需配校验 |
| 平台模板策略 | JoinQuant 等特定平台代码生成 | 跨平台策略抽象 | joinquant-skill | 平台输出必须 lint |
| 公式执行 | 快速条件筛选、批量 TopN | 复杂组合调仓 | quant-buddy-skill | 公式结果必须校验排序和参数 |
| ISQ 信号结构 | 新闻/事件/传导链/置信度 | 纯价格规则 | Awesome Finance Skills / DeepEar | 报告和信号库采用 |
| Suggestion pool | AI 建议、模拟盘、组合约束 | 真实交易直接执行 | PanWatch | AI 建议先入池，用户确认后再动作 |
| 受约束组合优化 | 多个信号或持仓需转为目标权重 | 单标的临时观点、未定义风险/约束 | PyPortfolioOpt | long-only、权重/行业上限、现金和换手成本起步 |
| 事件驱动执行适配器 | 已有审批、对账和 paper broker 的订单生命周期 | Agent 直接下单、无持久化审计的原型 | vn.py | 用统一 Order/Trade/Position 事件；Gateway 仅在后期适配 |

### 推荐默认

```text
长期策略契约       -> StrategyDef
配置型策略         -> YAML
平台代码输出       -> 模板 + AST lint
快速筛选           -> 公式执行 + TopN 审计
事件/新闻信号       -> ISQ
AI 建议            -> suggestion pool
```

## 9. 第一阶段取舍矩阵

| 选择题 | 第一阶段推荐 | 暂缓 |
|--------|--------------|------|
| 数据服务 | static JSON + SQLite + Parquet 窄表 | 大规模实时行情服务 |
| 外部接口 | JSON CLI + Agent/Skill Gateway envelope | 直接散落 HTTP 调用 |
| Agent 编排 | Moderator + BaseAgent | 完整 LangGraph 大状态机 |
| UI | 工作台首页 + 个股页 + 运行页 + 盯盘页 + 工具调用页 | 大而全导航和营销首页 |
| 审计 | Tool envelope + data quality + snapshot date/schema/cache | 只审最终报告文本 |
| 策略 | StrategyDef + 四向信号 + lint + formula audit | 自动实盘交易 |
| 报告 | Markdown/HTML + citation + data quality banner | 无引用自然语言报告 |

## 10. 反模式清单

| 反模式 | 为什么危险 | 应对 |
|--------|------------|------|
| 直接让 Agent 拼 HTTP 请求 | 无 provenance、难审计、认证边界混乱 | 先封 JSON CLI 或 gateway |
| 先做复杂 UI 再补数据契约 | UI 会依赖隐式字段，后续难改 | 先写 `DATA_CONTRACTS.md` |
| 把 AI 建议直接当交易动作 | 风控、合规、责任边界不清 | suggestion pool + 用户确认 |
| 所有数据都进 PostgreSQL | 个人/本地场景部署重，分析成本高 | Parquet/DuckDB/SQLite/static JSON 分层 |
| 所有实时都用 WebSocket | 复杂度高，很多只读场景不需要 | SSE 或 daemon polling |
| 报告只有结论没有来源 | 不能审计，不能复查 | citation key + claim-to-source |
| “优雅降级”吞掉结构错误 | 缺陷长期潜伏 | critical 字段 hard gate |
| 工具只记录最终自然语言 | 无法重放和定位错误 | 记录 command/skillId + params + raw hash |

## 11. 当前项目默认架构建议

### HermesAlpha 默认组合

```text
Data: static JSON + SQLite + Parquet/DuckDB
Tools: JSON CLI + Agent Gateway + SKILL.md + future MCP
Agent: Moderator meeting room + BaseAgent watch lifecycle
Strategy: StrategyDef + formula execution + suggestion pool
UI: React workbench + HTML/Markdown report export
Audit: ToolCallEnvelope + Agent scope audit + citation store + data quality banner
```

### ashare-audit 默认组合

```text
Data: Parquet narrow audit tables + static snapshot checks
Tools: ToolCallEnvelope for CLI/MCP/Gateway/Skill
Strategy: AST lint + future function + TopN/order validation
Report: claim-to-source + base/adjusted forecast audit
UI: RunFlow + tool call page + data quality page
Output: issue list + suggested fix + regression checklist
```

## 12. 最简决策口诀

```text
能静态，就先静态。
能 CLI，就别散落 HTTP。
能记录 envelope，就别只留结论。
能 SQLite，就别先上重库。
能 Moderator，就别急着 LangGraph。
能 suggestion pool，就别碰真实交易。
能显示来源，就别只显示答案。
```

## 13. 决策记录模板

当实际选择偏离“推荐默认”，或决定引入 PostgreSQL、LangGraph、WebSocket、交易执行等高成本能力时，应新增一条决策记录。

```markdown
# ADR-<编号>：<决策标题>

- 状态：提议 / 已接受 / 已替代 / 已撤销
- 日期：YYYY-MM-DD
- 决策范围：HermesAlpha / ashare-audit / 共享基础设施

## 背景
当前问题、已有约束和触发升级的真实证据。

## 候选方案
列出 2~4 个可行方案及其成本、风险和适用条件。

## 决策
选择的方案，以及为什么它优于其他候选。

## 后果
新增复杂度、迁移成本、运维要求和需要补充的测试。

## 回退条件
什么指标或事件出现时，应撤销或替换该决策。

## 验证
用于证明决策有效的测试、指标和观察周期。
```

决策记录应引用本矩阵中的相关分叉和项目证据。未出现真实升级条件时，继续使用默认方案。
